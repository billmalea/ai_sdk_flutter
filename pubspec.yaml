name: ai_sdk_flutter
description: A Flutter package that consumes Vercel AI SDK v5 streams, providing chat functionality, tool calling, and streaming capabilities for AI-powered applications.
version: 0.1.3
homepage: https://github.com/billmalea/ai_sdk_flutter
repository: https://github.com/billmalea/ai_sdk_flutter
issue_tracker: https://github.com/billmalea/ai_sdk_flutter/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  equatable: ^2.0.5
  meta: ^1.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.0
  build_runner: ^2.4.0
  test: ^1.24.0
